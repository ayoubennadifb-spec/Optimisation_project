[pytest]
testpaths = tests
addopts = -q
# PuLP émet une DeprecationWarning (LpProblem.constraints comme mapping) à chaque
# accès aux duales : bruit inoffensif (pulp est figé <4), masqué pour la lisibilité.
filterwarnings =
    ignore::DeprecationWarning
